---
hide:
  - toc
---

# 3. Load the four depth rails

<p class="pf-step-count">Assembly · Step 3 of 19</p>

<p class="pf-profile-banner" data-guide-device="trimui-smart-pro" data-layout-id="chassis-core-v2"><strong>Compatible DUT:</strong> TrimUI Smart Pro <span>·</span> <code>chassis-core-v2</code></p>

The **depth rails** run from the operator side to the device side. Each receives
one bar for a gantry joint and one blue-marked replacement. The lower
operator-right rail also receives four active power-strip bars.

<div class="pf-step-layout" markdown="1">
<div class="pf-step-visual">
  <figure>
    <img src="../../../../../../assets/generated/test-node-chassis/legacy/preload-map.png" alt="Transparent complete chassis showing orange active sliding nut bars and blue parked spare bars inside aluminum rails">
    <figcaption>Every depth rail receives one orange gantry bar and one blue-tagged spare. <code>DEPTH-R-L</code> also receives the four orange power-strip bars shown along its inside face.</figcaption>
  </figure>
</div>
<div class="pf-step-copy" markdown="1">

## Get these parts

<ul class="pf-part-list">
  <li><span class="pf-key pf-key--black" aria-hidden="true"></span><span><strong>4 × 318 mm depth rails</strong><small>Left lower, left upper, right lower, right upper</small></span></li>
  <li><span class="pf-key pf-key--orange" aria-hidden="true"></span><span><strong>8 × active short sliding nut bars</strong><small>Four gantry positions plus four power-strip positions</small></span></li>
  <li><span class="pf-key pf-key--blue" aria-hidden="true"></span><span><strong>4 × short sliding nut bars to park as spares</strong><small>Mark their screw heads with blue removable tape</small></span></li>
  <li><span class="pf-key pf-key--magenta" aria-hidden="true"></span><span><strong>12 × M3 screws and wide washers</strong><small>Eight removable handles plus four spare retainers</small></span></li>
</ul>

## Do this

1. Lay the rails as left/right pairs with their tape labels readable.
2. Find the groove that will face the empty center of the chassis. The two
   left rails use their **right-facing** groove; the two right rails use their
   **left-facing** groove.
3. Slide one gantry bar and one blue-marked spare into that same groove on
   every rail.
4. In `DEPTH-R-L`, add **four more active bars** to the same inboard groove.
   These four will mount the power-strip blocks in Step 17.
5. Move each spare toward the future **device-side** end. Leave enough clear
   rail for the metal corner connector tongue.
6. Tighten only the spare's screw until its washer stops the bar from sliding
   or rattling.
7. Leave all eight active bars loose on screw handles. Four meet gantry joint
   plates; four remain together in `DEPTH-R-L` for the strip.

</div>
</div>

<div class="pf-step-check" markdown="1">

**Before you continue:** `DEPTH-L-L`, `DEPTH-L-U`, and `DEPTH-R-U` each contain
one loose gantry bar and one parked spare. `DEPTH-R-L` contains those two plus
four loose power-strip bars. That is **12 bars in this step: 8 active + 4
parked**.

</div>

<nav class="pf-step-nav" aria-label="Assembly step navigation">
  <a href="../02-load-width-rails/">← Width rails</a>
  <span>Step 3 of 19</span>
  <a href="../04-splice-uprights/">Next: Splice uprights →</a>
</nav>
